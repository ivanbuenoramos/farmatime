# Dependencias
node_modules/

# Logs
npm-debug.log*
firebase-debug.log*
pubsub-debug.log*
ui-debug.log*

# Runtime local del emulador
.runtimeconfig.json

# NOTA: los .env de functions (.env.local, .env.farmatime-app) se versionan a
# propósito: no contienen secretos (esos viven en Secret Manager).
